package com.levelzero.village.building;

/**
 *  An empty interface to implement for all buildings in the village. This allows us to treat all buildings as the same type, even if they have different properties and behaviors.
 */
public interface Building {
}
